<UserControl x:Class="Hextasy.CardWars.Presentation.Controls.PlayMonsterCardActionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../DataTemplates/Styles.xaml"/>
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </UserControl.Resources>
  <Grid>
    <Border Style="{StaticResource ActionLogBorderStyle}">
      <Image Source="{Binding PreviewImage}"/>
      <Border.ToolTip>
        <ToolTip BorderThickness="0" Background="Transparent">
          <Image Source="{Binding PreviewImage}"/>
        </ToolTip>
      </Border.ToolTip>
    </Border>
  </Grid>
</UserControl>
